// Simple output
OUTPUT('My first ECL code', NAMED('TestMe'));

// Let's add 2+2
OUTPUT(2 + 2, NAMED('Simple_Math'));

// Let's output a variable 
callIt := (2 + 12) * 3;
str   := 'Result is: ';
OUTPUT(str + callIt);

// TODO
/* Display the following: 
   My random value is : xx and my fave day is Friday.
   Hint: use RANDOM() function to get a random value.
*/





